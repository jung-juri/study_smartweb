		<컴퓨터에게 명령을 효율적으로 내리기 위한 수단>
		숫자
		문자
		상수 = 변하지 않는 수
		변수
		배열
		연산자
		함수
		조건문
		반복문
		객체
		상속
.js
자바 (inline,internal 속성)
따옴표를 감싸면 문자, 숫자는 그대로
인터프린터: 위에서부터 아래로 읽어줌(html,css와 동일)

<내장메소드 = 내장함수:결과값 확인을 위한,출력을 위한(내가만들지 않은 함수(기능))>
alert: 알림창
console.log: 개발자 도구에서만 확인가능.
	- 작은따옴표를 밖에 감싸면 안에 큰따옴표를 문자로 인식해서 출력
	- 문자/숫자만 인식, 정보형, 태그는 문자그대로출력
document.write(): 텍스트 출력(?)
typeof():안의 값이 숫자인지 ,. 문자인지
prompt():입력창/ 입력한 값은 기본적으로 string(문자)
	예시) year = Number(year); -숫자로 변환

 배열 = array = []
 a = new array
 var ~~ = [] (0부터 시작함)

Math.floor() 소수점 절삭(문자이더라도, 소수점이 있으면 인식할수없음!!!!!)
Math.random() 난수 생성(실수(소수점)형태를 랜덤으로)
~~.length (~~의 안에 있는 값을 세어줌,1부터 시작..)


선언된이름.unshift(~~) ~~가 배열에 추가

if문, for문 서로서로 다 들어갈수 있음.

*syntaxerror:오타에러*

headfirst javascript(흑형)
javascript 코뿔소
headfirst jquery
jquery 쿡북
만들면서 이해하는 javascript + jquery 입문 + 실전북


